Dropdown escuro para a área /admin. Mesma anatomia visual do Input.

```jsx
<Select label="Linha de produto" placeholder="Selecione"
        options={["Frotas","Agro","Detailing","Animal"]} />
```

`options` aceita strings ou `{value, label}`. Estilo idêntico ao `Input` (foco azul, navy escuro).
